#include<stdio.h>
int main()
{
    int n;
    scanf("%d",&n);
    printf("enter a value:");
    if(n>0)
    {
        printf("Positive");
    }
    else if(n<0)
    {
        printf("Negative");
    }
    else
    {
        printf("zero");
    }
    return 0;
}
